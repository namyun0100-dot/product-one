# 작업 규칙 (CosmicPaw)

## 필수 원칙
- 모든 블로그 글은 **한국어(KO) + 영어(EN) 버전**을 동시에 작성한다.
- 두 버전 모두 **정확한 위치에 저장**하고, `blog.html` 목록과 `sitemap.xml`에 **둘 다** 반영한다.
- 각 글의 `canonical`, `og:url`, `twitter` 메타, `ld+json`은 **해당 버전 URL**로 정확히 맞춘다.
- 블로그 글은 **윗부분 날짜와 메타만 바꾸는 식으로 갱신하지 않는다.** 날짜가 바뀌면 본문 전체를 현재 주차 흐름에 맞게 다시 쓴다.

## 검색 및 근거 원칙
- 주간 운세/주간 금전운처럼 **날짜 범위가 있는 글은 작성 전에 반드시 검색으로 기준 앵커를 확인**한다.
- 검색 없이 임의로 톤만 바꿔 발행하지 않는다.
- 최소한 아래 2가지는 확인한 뒤 반영한다.
  - **날짜 앵커:** 다음 주 정확한 날짜 범위
  - **주간 흐름 앵커:** 새달/보름달/상현달 같은 천문 이벤트, 시즌 전환, 달력상 흐름 등 본문 방향을 잡을 근거
- 외부 검색으로 확인한 사실과, 그 사실을 바탕으로 한 해석/문장 작성은 구분해서 생각한다.
  - **날짜/이벤트 자체는 확인된 사실**
  - **각 별자리별 해석과 문장 구성은 편집적 해석**

## 주간 운세 키워드 규칙
- “이번 주 운세 키워드”는 **항상 다음 주 날짜 범위**로 작성한다.
  - 예: `2월 9–15일`처럼 표기
- 주간 글은 **미리 작성/예약 성격**으로 톤을 맞춘다.
- **이번 주 운세 키워드 글은 같은 파일을 매주 갱신(갈아끼우기)**한다.
  - KO: `blog_posts/weekly-keywords.html`
  - EN: `blog_posts/weekly-keywords-en.html`
- 같은 파일을 갱신할 때도 다음 항목을 전부 바꾼다.
  - 제목
  - 설명 메타
  - 키워드 메타
  - 본문 전체
  - `dateModified`
  - 필요 시 `datePublished`
  - 관련 글 링크

## 주간 금전운 규칙
- **주간 금전운은 매주 새 글로 발행**한다.
- 파일명은 **해당 다음 주 날짜 범위가 드러나도록** 만든다.
  - 예: `weekly-money-zodiac-04-20-04-26.html`
  - EN도 같은 범위로 맞춘다.
- 금전운 허브(모음) 페이지에 **최신 글을 추가**하고, 필요 시 이전 글 목록을 정리한다.
- 주간 금전운도 **다음 주 날짜 범위**로 작성한다.
- 새 글을 만들 때도 다음 항목을 전부 새 주차 기준으로 작성한다.
  - 제목
  - 설명 메타
  - 키워드 메타
  - 본문 전체
  - `canonical`
  - `og:url`
  - `twitter:url`
  - `ld+json`

## 공통 반영 규칙
- 이번 주 운세 키워드와 주간 금전운은 **KO/EN 동시 업데이트**한다.
- `blog.html` 목록과 `sitemap.xml`에 **둘 다 반영**한다.
- 아래 연결 파일도 함께 확인한다.
  - `index.html`
  - `main.v4.js`
  - 금전운 허브/아카이브 페이지
- 홈/블로그/허브/사이트맵 중 하나라도 이전 최신 링크를 그대로 두지 않는다.

## 메타데이터 규칙
- `canonical`, `og:url`, `twitter:url`, `mainEntityOfPage`는 **현재 문서의 실제 URL**과 정확히 일치해야 한다.
- `hreflang`은 KO/EN 짝을 맞춘다.
- `datePublished`와 `dateModified`는 **실제 발행/수정 시점**과 맞춘다.
- KO 문서 메타에 EN URL을 넣거나, EN 문서 메타에 KO URL을 넣지 않는다.

## 작성 품질 규칙
- 날짜만 바꾸고 기존 문단을 그대로 두지 않는다.
- 각 주차 글은 적어도 아래 3개를 새로 반영한다.
  - 주간 전체 흐름 설명
  - 12별자리별 포인트 문장
  - 행동/체크리스트/주의사항
- 직전 주차와 문장 구조, 표현, 결론이 과하게 같지 않게 쓴다.
- 독자가 읽었을 때 “이번 주차를 위해 새로 쓴 글”처럼 보여야 한다.

## 작업 순서 규칙
- 작업 시작 전에 이 문서(`AGENTS.md`)를 먼저 읽고 준수한다.
- 주간 글 작업 시에는 `WEEKLY_CONTENT_WORKFLOW.md`를 함께 확인한다.
- 별자리·천체 심화 글 또는 신규 콘텐츠 묶음을 기획·작성할 때는 `CONTENT_STRATEGY.md`를 함께 확인한다.
- 작업 후에는 최소한 아래를 다시 확인한다.
  - 최신 주차 링크가 맞는지
  - KO/EN 둘 다 반영됐는지
  - 사이트맵에 새 URL과 수정일이 들어갔는지
  - 이전 최신 글 링크가 필요한 위치에서는 보존됐는지
